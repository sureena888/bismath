function isEven(vector) {
    for num in vector {
        (num % 2 != 0) -> {
            output false;
        } otherwise {
            put "So far, so good!";
        }
    }
    output true;
}

fix example = <2,4,6,7,8>; // example is read-only b/c of fix
put isEven(example);