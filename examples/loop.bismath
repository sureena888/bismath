vector = <2,4,6,8,10>;
matrix = [[1,2,3], [4,5,6], [7,8,9]];
x = 10;


for value in vector {
    put value;
}

// Maybe use @ for string interpolation.
for row in matrix {
    for col in row {
        // put "(row,col): (@row,@col)";
        put matrix[row][col];
    }
} 

while x > 0 {
    put x;
    x = x - 1;
}

